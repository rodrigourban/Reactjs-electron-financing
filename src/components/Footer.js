import React from "react";
import PropTypes from "prop-types";

const Footer = (props) => {
  return (
    <div style={{ marginTop: "2rem" }}>
      <small>Made by RIGO. 2021.</small>
    </div>
  );
};

Footer.propTypes = {};

export default Footer;
